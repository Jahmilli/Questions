//You'll have to simulate the video game's character selection screen behaviour, more specifically the selection grid. 
import java.util.*;

/*
Selection Grid Layout in text:

| Ryu  | E.Honda | Blanka  | Guile   | Balrog | Vega    |
| Ken  | Chun Li | Zangief | Dhalsim | Sagat  | M.Bison |

Input
the list of game characters in a 2x6 grid;
the initial position of the selection cursor (top-left is (0,0));
a list of moves of the selection cursor (which are up, down, left, right);
Output

the list of characters who have been hovered by the selection cursor after all the moves 
(ordered and with repetition, all the ones after a move, wether successful or not, see tests);
Rules

Selection cursor is circular horizontally but not vertically!
 */

 
 class Solution {
	public static void main(String[] args) {
		String[] moves = new String[] { "left", "left", "left", "left", "left", "left", "left", "left" };
		 String[][] fighters = new String[][]{
             new String[] { "Ryu", "E.Honda", "Blanka", "Guile", "Balrog", "Vega" },
             new String[] { "Ken", "Chun Li", "Zangief", "Dhalsim", "Sagat", "M.Bison" },
		 };
		 String[] newChar = streetFighterSelection(fighters, new int[]{0,0}, moves);
		 for(String s : newChar)
			 System.out.print(s + " ");
	}         
  
	 public static String[] streetFighterSelection(String[][] fighters, int[] position, String[] moves){
		String[] characters = new String[moves.length];
		int counter = 0;
		for(String s : moves) {
			 switch(s) {
				 case "up": position[0] = 0; break;
				 case "right": position[1] = position[1] < 5 ? position[1] += 1 : 0; break;
				 case "down": position[0] = 1; break;
				 case "left": position[1] = position[1] > 0 ? position[1] -= 1 : 5; break;
			 }
			 System.out.println(position[0] + " " + position[1]);
			 characters[counter] = fighters[position[0]][position[1]];
			 counter++;
	 	}	
		 return characters;
	 }
}
